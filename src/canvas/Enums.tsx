export enum PatternPathType {
    UNDEFINED,
    Seam,
    Fold,
    Edge,
}

export enum ToolType {
    StraightLine,
    Freeline
}

export enum Direction {
    UNDEFINED,
    Increase,
    Decrease
}
